# 📋 Гайд по интеграции Google Sheets и развертыванию на Netlify

## Шаг 1: Подготовка Google Sheets

### 1.1 Создание таблицы с продуктами

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу "Chic Style Hub Products"
3. Заполните заголовки (первая строка):
   ```
   ID | Изображение | Название | Цена | Старая цена | Размеры | Категория | Новое?
   ```

### 1.2 Формат данных в Google Sheets

**Лист 1 - Продукты (Товары):**
| ID | Изображение | Название | Цена | Старая цена | Размеры | Категория | Новое? |
|---|---|---|---|---|---|---|---|
| 1 | https://example.com/image1.jpg | Пальто оверсайз | 8990 | 12900 | S,M,L,XL | Пальто | Да |
| 2 | https://example.com/image2.jpg | Свитер вязаный | 3490 | | S,M,L | Свитера | |

**Лист 2 - Контакты:**
| Телефон | Email | Telegram | Instagram | Адрес |
|---|---|---|---|---|
| +7 (XXX) XXX-XX-XX | email@example.com | @yourhandle | @yourhandle | г. Москва, ул. XXX |

**Важно:**
- Все ссылки на изображения должны быть полные (https://...)
- Размеры перечисляйте через запятую: `S,M,L,XL`
- Для "Новое?" пишите "Да" или оставляйте пусто
- Используйте реальные числа без пробелов для цен

### 1.3 Публикация таблицы

1. Нажмите **Общий доступ** (правый верхний угол)
2. Выберите **Изменить** → **Все могут просматривать как гость**
3. Скопируйте URL таблицы (из адресной строки):
   ```
   https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit#gid=0
   ```

## Шаг 2: Генерация URL экспорта

Из стандартного URL получите CSV-ссылку:

**Замените в этом шаблоне:**
```
https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/export?format=csv&gid={SHEET_ID}
```

**Примеры:**
- Продукты: `gid=0` (первый лист)
- Контакты: `gid=123456` (другой лист - посмотрите в URL листа)

**Полный пример:**
```
https://docs.google.com/spreadsheets/d/1mABC123XYZ456/export?format=csv&gid=0
```

## Шаг 3: Локальная настройка переменных окружения

1. Скопируйте файл:
   ```bash
   cp .env.example .env.local
   ```

2. Отредактируйте `.env.local` с вашей CSV-ссылкой:
   ```env
   VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_ID/export?format=csv&gid=0
   ```

3. Протестируйте локально:
   ```bash
   bun run dev
   ```

## Шаг 4: Развертывание на Netlify

### 4.1 Первый раз (через веб-интерфейс)

1. Если еще не зарегистрированы:
   - Откройте [netlify.com](https://netlify.com)
   - Зарегистрируйтесь через GitHub (рекомендуется)

2. Нажмите **"New site from Git"**

3. Выберите **GitHub** и авторизуйтесь

4. Выберите ваш репозиторий `chic-style-hub`

5. Настройки:
   - **Build command:** `bun run build`
   - **Publish directory:** `dist`
   - **Environment variables:** Нажмите на "Advanced build settings"

6. Добавьте переменную окружения:
   - **Key:** `VITE_GOOGLE_SHEET_URL`
   - **Value:** `https://docs.google.com/spreadsheets/d/YOUR_ID/export?format=csv&gid=0`

7. Нажмите **Deploy**

### 4.2 Последующие обновления

Просто делайте `git push` в основную ветку - Netlify автоматически пересоберет и опубликует сайт.

## Шаг 5: Использование в компонентах

### Вариант 1: Загрузка при запуске приложения

Отредактируйте [src/App.tsx](../src/App.tsx):

```typescript
import { useEffect, useState } from 'react';
import { fetchProductsFromSheets, fetchContactsFromSheets } from '@/services/sheetsService';

export default function App() {
  const [products, setProducts] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function loadData() {
      const data = await fetchProductsFromSheets();
      setProducts(data);
      setLoading(false);
    }
    loadData();
  }, []);

  if (loading) return <div>Загрузка...</div>;

  return (
    // используйте products
  );
}
```

### Вариант 2: Использование React Query (рекомендуется)

```typescript
import { useQuery } from '@tanstack/react-query';
import { fetchProductsFromSheets } from '@/services/sheetsService';

export function useProducts() {
  return useQuery({
    queryKey: ['products'],
    queryFn: fetchProductsFromSheets,
    staleTime: 1000 * 60 * 5, // 5 минут
  });
}
```

## Шаг 6: Автоматическое обновление данных

### Вариант 1: Redeploy хуки (автоматический пересборка при изменении)

1. На Netlify откройте **Site settings** → **Build & deploy** → **Build hooks**
2. Нажмите **Add build hook**
3. Назовите его "Refresh from Sheets"
4. Скопируйте webhook URL
5. В Google Sheets установите [Apps Script](https://script.google.com):
   ```javascript
   function onEdit(e) {
     const webhookUrl = "YOUR_NETLIFY_WEBHOOK_URL";
     UrlFetchApp.fetch(webhookUrl, {method: "post"});
   }
   ```

### Вариант 2: Периодическое обновление на клиенте

Используйте React Query с `refetchInterval`:

```typescript
useQuery({
  queryKey: ['products'],
  queryFn: fetchProductsFromSheets,
  refetchInterval: 1000 * 60 * 5, // 5 минут
});
```

## Проблемы и решения

### ❌ Ошибка CORS
**Причина:** Google Sheets блокирует прямые запросы  
**Решение:** 
- Используйте URL `/export?format=csv` (не JSON API)
- Убедитесь, что таблица доступна "всем"

### ❌ Изображения не загружаются
**Причина:** Неправильный URL  
**Решение:**
- Ссылки должны быть полные: `https://...`
- Тестируйте ссылку в браузере

### ❌ Данные не обновляются после изменения
**Решение:**
- Кэширование: измените `staleTime` в React Query
- Или используйте Netlify build hooks
- Или вручную нажмите "Trigger deploy" на Netlify

## Полезные ссылки

- [Google Sheets API Docs](https://developers.google.com/sheets/api)
- [Netlify Docs](https://docs.netlify.com)
- [Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)

## Примечания

- Данные кэшируются браузером - для их обновления используйте стратегию из пункта "Автоматическое обновление"
- На бесплатном плане Netlify: 300 минут в месяц, оптимизируйте build время
- Google Sheets имеет лимит на API запросы - используйте кэширование

---

**Готово!** Теперь вы можете обновлять товары, цены и контакты прямо в Google Sheets, а сайт будет отражать изменения автоматически. 🎉
