# ✅ Как правильно настроить VITE_GOOGLE_SHEET_URL

## 🎯 Проблема

Сайт не отображает товары, потому что:
- ❌ Переменная `VITE_GOOGLE_SHEET_URL` не установлена
- ❌ ИЛИ установлена неправильная ссылка
- ❌ ИЛИ таблица не опубликована как публичная

## ✅ Решение

### Шаг 1: Получить правильную ссылку на CSV

1. **Откройте Google Sheets таблицу** с товарами
2. **Посмотрите на URL** в адресной строке:
   ```
   https://docs.google.com/spreadsheets/d/1mABC123XYZ456789/edit#gid=0
                                         ↑ ЭТО ID таблицы
   ```

3. **Создайте CSV ссылку** по этому шаблону:
   ```
   https://docs.google.com/spreadsheets/d/{ID}/export?format=csv&gid=0
   ```

**Пример:**
```
Исходный URL: https://docs.google.com/spreadsheets/d/1mABC123XYZ456789/edit#gid=0
CSV URL:      https://docs.google.com/spreadsheets/d/1mABC123XYZ456789/export?format=csv&gid=0
```

### Шаг 2: Опубликовать таблицу как публичную

1. Нажмите **Share** (правый верхний угол)
2. Измените на **"Anyone can view"** (или "Everyone with the link")
3. Скопируйте ссылку или просто закройте окно

### Шаг 3: Установить переменную локально

#### Способ 1: Прямое редактирование

1. Откройте файл `.env.local` (или создайте его)
2. Добавьте/измените строку:
   ```env
   VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_ID/export?format=csv&gid=0
   ```
3. Сохраните файл
4. Перезагрузите сайт: `bun run dev`

#### Способ 2: Из шаблона

```bash
# Копировать шаблон
cp .env.example .env.local

# Отредактировать (откройте .env.local в редакторе)
# И замените YOUR_SPREADSHEET_ID на реальный ID
```

### Шаг 4: Проверить что работает

1. **Проверить в браузере:**
   - Откройте `http://localhost:8080`
   - Должны отображаться товары из Google Sheets
   - Если видите ошибку - смотрите браузерную консоль (F12)

2. **Проверить CSV ссылку:**
   - Вставьте ссылку в адресную строку браузера
   - Должен скачаться CSV файл

3. **Проверить переменную окружения:**
   - Откройте DevTools (F12)
   - Console tab:
   ```javascript
   console.log(import.meta.env.VITE_GOOGLE_SHEET_URL)
   // Должна вывести вашу ссылку
   ```

## ⚠️ Распространенные ошибки

| Ошибка | Причина | Решение |
|--------|---------|--------|
| Товары не загружаются | Переменная не установлена | Создайте `.env.local` с ссылкой |
| CORS ошибка | Неправильный формат ссылки | Используйте `/export?format=csv` |
| 404 ошибка | Таблица удалена или неправильный ID | Проверьте URL таблицы |
| Пустой список | Таблица не опубликована | Share → Anyone can view |
| Старые данные | Кэш браузера | Ctrl+Shift+Delete для очистки |

## 🔍 Отладка

### Если ничего не работает:

1. **Проверьте консоль браузера (F12)**
   ```
   Network tab → найдите запрос к Google Sheets
   Он должен иметь статус 200
   ```

2. **Проверьте .env.local**
   ```bash
   cat .env.local
   # Должна быть полная ссылка типа:
   # VITE_GOOGLE_SHEET_URL=https://docs.google.com/...
   ```

3. **Перезагрузитесь несколько раз**
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите заново
   bun run dev
   ```

4. **Проверьте таблицу**
   - Откройте таблицу в браузере
   - Убедитесь, что данные там
   - Проверьте, что таблица публичная

## ✅ Правильная настройка выглядит так

**.env.local файл:**
```env
VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/1mABC123XYZ456789/export?format=csv&gid=0
```

**Google Sheets:**
- ✅ Таблица содержит товары
- ✅ Заголовки: ID, Изображение, Название, Цена, ...
- ✅ Таблица опубликована (Share → Anyone can view)
- ✅ Ссылка экспорта скопирована правильно

**Результат:**
- ✅ Товары отображаются на сайте
- ✅ Обновляются каждые 5 минут
- ✅ Нет ошибок в консоли

## 🎯 ГЛАВНОЕ

**ЗАМЕНИТЕ:** `YOUR_SPREADSHEET_ID` на реальный ID из вашей таблицы

**Пример неправильно:**
```
https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID/export?format=csv&gid=0
```

**Пример правильно:**
```
https://docs.google.com/spreadsheets/d/1mABC123XYZ456789/export?format=csv&gid=0
```

---

Готово! Теперь товары будут загружаться только из Google Sheets! 🚀
