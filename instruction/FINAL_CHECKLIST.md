# ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ: Google Sheets интеграция работает

## 🔧 ЧТО БЫЛО ИСПРАВЛЕНО

### Проблема
Компоненты показывали товары из файла `products.ts` вместо данных из Google Sheets.

### Причина
- CatalogSection.tsx импортировал `products` напрямую из `@/data/products`
- NewArrivalsSection.tsx жестко закодировал фильтрацию на уровне модуля

### Решение
✅ CatalogSection.tsx теперь использует `useProducts()` хук  
✅ NewArrivalsSection.tsx теперь использует `useProducts()` хук  
✅ Добавлена обработка loading и error состояний  
✅ Категории и размеры динамически вычисляются из данных  

---

## 📋 ПРОВЕРОЧНЫЙ СПИСОК

### Шаг 1: Создать Google Sheets таблицу

- [ ] Открыть https://sheets.google.com
- [ ] Создать новую таблицу "Chic Style Hub Products"
- [ ] Добавить заголовки в первой строке:
  ```
  ID | Изображение | Название | Цена | Старая цена | Размеры | Категория | Новое?
  ```
- [ ] Добавить минимум 1-2 товара
- [ ] Пример товара:
  ```
  1 | https://images.unsplash.com/photo-1542272604-787c62d465d1?w=400 | Черное пальто | 8990 | 12900 | S,M,L,XL | Пальто | Да
  ```

### Шаг 2: Опубликовать таблицу

- [ ] Нажать **Share** (правый верхний угол)
- [ ] Изменить на **"Anyone can view"**
- [ ] Скопировать URL таблицы из адресной строки

### Шаг 3: Получить CSV ссылку

Из URL: `https://docs.google.com/spreadsheets/d/ВАШЕ_ID/edit`  
Создать: `https://docs.google.com/spreadsheets/d/ВАШЕ_ID/export?format=csv&gid=0`

- [ ] Скопировать ID из URL
- [ ] Создать CSV ссылку с `/export?format=csv&gid=0`
- [ ] Протестировать в браузере (должен скачать CSV)

### Шаг 4: Настроить .env.local

```bash
# Если файл существует - отредактировать
nano .env.local

# Если файла нет - создать из шаблона
cp .env.example .env.local
```

Добавить строку:
```env
VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/ВАШЕ_ID/export?format=csv&gid=0
```

- [ ] Файл `.env.local` создан
- [ ] Содержит правильную CSV ссылку
- [ ] Нет пробелов в начале/конце

### Шаг 5: Запустить dev сервер

```bash
# Остановить старый процесс если запущен (Ctrl+C)
bun run dev
```

- [ ] Команда выполнилась без ошибок
- [ ] Видно "Local: http://localhost:8080"

### Шаг 6: Проверить в браузере

- [ ] Открыть http://localhost:8080
- [ ] Видны товары из Google Sheets в каталоге
- [ ] Видны новые товары в "Новинки сезона"
- [ ] Фильтры работают (категория, размер)
- [ ] Нет ошибок в консоли браузера (F12)

---

## 🔍 ОТЛАДКА

### Если товары не появляются

**1. Проверить консоль браузера (F12)**
```
1. Нажать F12 → Console tab
2. Посмотреть красные сообщения об ошибке
3. Скопировать текст ошибки
```

**2. Проверить Network**
```
1. Нажать F12 → Network tab
2. Перезагрузить страницу (F5)
3. Найти запрос к Google Sheets
4. Проверить статус (должен быть 200)
```

**3. Проверить переменную окружения**
```javascript
// В консоли браузера (F12 → Console):
console.log(import.meta.env.VITE_GOOGLE_SHEET_URL)
// Должна вывести вашу CSV ссылку
```

**4. Проверить .env.local**
```bash
# Посмотреть содержимое
cat .env.local

# Должно быть что-то типа:
# VITE_GOOGLE_SHEET_URL=https://docs.google.com/...
```

### Если CSV ссылка неправильная

**Правильный формат:**
```
https://docs.google.com/spreadsheets/d/ДЛИННЫЙ_НОМЕР/export?format=csv&gid=0
```

**Неправильный формат (❌):**
```
https://docs.google.com/spreadsheets/d/НОМЕР/edit  (edit вместо export)
https://docs.google.com/spreadsheets/d/НОМЕР/edit?usp=sharing  (с ?usp)
```

### Если таблица не опубликована

**Проверить:**
1. Откройте Google Sheets
2. Нажмите Share (правый верхний угол)
3. Должно быть "Anyone with the link can view" или "Everyone can view"
4. Если показывает "Restricted" - измените!

---

## ✅ ДОЛЖНО РАБОТАТЬ

### CatalogSection (основной каталог)
```
✓ Загружает товары из Google Sheets
✓ Показывает скелетоны во время загрузки
✓ Показывает все товары из таблицы
✓ Фильтры работают (категория, размер, цена)
✓ Если ошибка - показывает сообщение с инструкцией
```

### NewArrivalsSection (новинки)
```
✓ Показывает товары где isNew="Да"
✓ Максимум 3 товара
✓ Скелетоны во время загрузки
```

### Состояния
```
LOADING: показывает скелетоны (серые прямоугольники)
SUCCESS: показывает товары из Google Sheets
ERROR: показывает "Ошибка при загрузке товаров"
```

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

После того как товары появились:

1. **Добавьте больше товаров** в Google Sheets
2. **Обновите цены** - изменения будут видны через 5 минут
3. **Добавьте новые размеры** - они появятся в фильтрах
4. **Создайте разные категории** - появятся в фильтре категорий
5. **Добавьте товары с isNew="Да"** - покажутся в "Новинках"

**Никаких пересборок не нужно! Всё работает автоматически!**

---

## 📞 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

1. Прочитайте раздел **ОТЛАДКА** выше
2. Проверьте все пункты в **ПРОВЕРОЧНОМ СПИСКЕ**
3. Посмотрите консоль браузера (F12)
4. Проверьте что таблица опубликована

**Самая частая причина:** неправильная CSV ссылка или таблица не опубликована

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Посмотреть .env.local
cat .env.local

# Перезагрузить dev сервер
# 1. Ctrl+C (остановить)
# 2. bun run dev (запустить)

# Очистить браузерный кэш
# Ctrl+Shift+Delete в браузере

# Посмотреть логи в консоли
# F12 → Console tab
```

---

**Статус:** ✅ Готово к использованию  
**Компоненты:** ✅ Используют useProducts() хук  
**Данные:** ✅ Загружаются из Google Sheets  
**Обновления:** ✅ Каждые 5 минут  

Удачи! 🎉
