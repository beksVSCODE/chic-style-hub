# ✅ Проблема решена!

## 🔧 Что было исправлено

Компоненты использовали импорт данных **напрямую** из `products.ts` вместо использования хука `useProducts()` который загружает данные из Google Sheets.

### Исправленные файлы:

1. **[CatalogSection.tsx](src/components/CatalogSection.tsx)**
   - ❌ Было: `import { products, categories... } from "@/data/products"`
   - ✅ Теперь: `const { data: products = [], isLoading, error } = useProducts()`
   - ✅ Добавлена обработка loading и error состояний
   - ✅ Динамически вычисляются категории и размеры из Google Sheets данных

2. **[NewArrivalsSection.tsx](src/components/NewArrivalsSection.tsx)**
   - ❌ Было: `const newProducts = products.filter...` (жестко закодировано)
   - ✅ Теперь: `const { data: products = [] } = useProducts()` + динамическая фильтрация
   - ✅ Добавлены скелетоны загрузки

---

## ✨ Как это работает теперь

```
Google Sheets таблица
        ↓
fetchProductsFromSheets() (парсит CSV)
        ↓
useProducts() хук (кэширует в React Query)
        ↓
CatalogSection & NewArrivalsSection (используют данные)
        ↓
Товары отображаются на сайте! ✓
```

---

## 🚀 Что нужно сделать

### 1. Проверить .env.local

Убедитесь, что файл `.env.local` существует и содержит правильную ссылку:

```bash
# Проверить есть ли файл
cat .env.local

# Должно быть:
VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_ID/export?format=csv&gid=0
```

**Если файла нет:**
```bash
cp .env.example .env.local
# Отредактируйте и вставьте правильную ссылку
```

### 2. Создать/обновить Google Sheets таблицу

**Заголовки (первая строка) обязательны:**
```
ID | Изображение | Название | Цена | Старая цена | Размеры | Категория | Новое?
```

**Пример данных:**
```
1 | https://example.com/image.jpg | Пальто оверсайз | 8990 | 12900 | S,M,L,XL | Пальто | Да
2 | https://example.com/image2.jpg | Свитер | 3490 | | S,M,L | Свитера |
```

**Важно:**
- ✅ Ссылки на изображения полные (https://...)
- ✅ Таблица опубликована (Share → Everyone can view)
- ✅ Размеры через запятую: `S,M,L,XL`
- ✅ Для "Новое?" пишите "Да" или оставляйте пусто

### 3. Перезагрузить сайт

```bash
# Остановить старый процесс (Ctrl+C в терминале)
# Запустить заново
bun run dev
```

### 4. Проверить что работает

1. **Откройте http://localhost:8080**
2. **Должны видеть товары из Google Sheets**
3. **Если ничего не видите - смотрите консоль браузера (F12)**

---

## 🔍 Отладка

### Если товары не загружаются

**1. Откройте браузерную консоль (F12)**
```
Console tab → посмотрите красные ошибки
```

**2. Проверьте Network запрос**
```
Network tab → найдите запрос к Google Sheets
Он должен иметь статус 200
```

**3. Проверьте переменную окружения**
```javascript
// В консоли браузера (F12 → Console):
console.log(import.meta.env.VITE_GOOGLE_SHEET_URL)
// Должна вывести вашу ссылку CSV
```

**4. Проверьте что таблица публичная**
- Откройте Google Sheets
- Share → должна быть "Everyone can view"

---

## ✅ Что должно работать

| Компонент | Данные | Статус |
|-----------|--------|--------|
| CatalogSection | Все товары + фильтры | ✅ Google Sheets |
| NewArrivalsSection | Товары с isNew=Да | ✅ Google Sheets |
| ProductCard | Каждый товар | ✅ Google Sheets |

---

## 📊 Состояния загрузки

Теперь компоненты правильно обрабатывают состояния:

```
1. LOADING → показывает скелетоны (Skeleton)
2. SUCCESS → показывает товары из Google Sheets
3. ERROR → показывает сообщение об ошибке с инструкцией
```

---

## 🎯 Главное

**Теперь все компоненты используют `useProducts()` хук, который загружает данные ИЗ GOOGLE SHEETS, а не из `products.ts`!**

Данные обновляются:
- ✅ При загрузке сайта
- ✅ Каждые 5 минут (автоматический refetch)
- ✅ При перезагрузке страницы (F5)

---

Готово! Теперь сайт использует ваши данные из Google Sheets! 🚀
