# 🏗️ Техническая архитектура

## Диаграмма архитектуры

```
┌─────────────────────────────────────────────────────────────────┐
│                     ПОЛЬЗОВАТЕЛЬ (Браузер)                      │
│                      chic-style-hub.netlify.app                │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
        ┌───────────▼──────────┐ ┌────────▼────────────┐
        │  React Components    │ │  React Query Cache  │
        │  (CatalogSection,    │ │  (staleTime: 5min)  │
        │   NewArrivals, etc)  │ │                     │
        └───────────┬──────────┘ └────────┬────────────┘
                    │                     │
        ┌───────────▼──────────────────────┴──────────┐
        │     Hooks (useProducts, useContacts)        │
        │  Загружают данные + парсят CSV + кэшируют  │
        └───────────┬──────────────────────────────────┘
                    │
        ┌───────────▼──────────────────────────────────┐
        │     Services (sheetsService.ts)              │
        │  Fetch CSV → Parse CSV → Return Objects     │
        └───────────┬──────────────────────────────────┘
                    │
                    │ (Network Request - раз в 5 минут)
                    │
        ┌───────────▼──────────────────────────────────┐
        │  Google Sheets CSV Export API                 │
        │  https://docs.google.com/spreadsheets/d/...  │
        │  /export?format=csv&gid=0                     │
        └──────────────┬───────────────────────────────┘
                       │
        ┌──────────────▼───────────────────────────────┐
        │     Google Sheets (Live Database)            │
        │     - Лист 1: Товары                         │
        │     - Лист 2: Контакты                       │
        │     - (Можно добавить больше листов)         │
        └──────────────────────────────────────────────┘
```

## Процесс обновления данных

```
1. ПЕРВЫЙ ЗАП УСКА (User opens website)
   ├─ Браузер загружает HTML
   ├─ React компоненты монтируются
   ├─ useProducts() вызывает useQuery()
   ├─ fetchProductsFromSheets() отправляет GET запрос
   ├─ Google Sheets отвечает CSV данными
   ├─ parseCSV() парсит строки
   ├─ convertToProduct() преобразует в объекты
   ├─ React Query кэширует результаты
   └─ UI обновляется ✓

2. ОБНОВЛЕНИЕ (User посещает сайт снова через <5 мин)
   ├─ Данные берутся из кэша React Query
   ├─ Никакой новый запрос НЕ отправляется
   ├─ UI обновляется мгновенно ✓

3. ОБНОВЛЕНИЕ (User посещает через >5 min)
   ├─ Кэш истекает (staleTime: 5 минут)
   ├─ React Query автоматически отправляет новый запрос
   ├─ Google Sheets отвечает актуальными данными
   ├─ Кэш обновляется
   └─ UI обновляется ✓

4. ОБНОВЛЕНИЕ В GOOGLE SHEETS
   ├─ Админ изменяет данные в Google Sheets
   ├─ CSV автоматически обновляется
   ├─ Сайт загружает новый CSV (в течение 5 минут)
   ├─ Пользователи видят обновленные данные
   └─ ВСЁ БЕЗ ПЕРЕСБОРКИ САЙТА ✓
```

## Структура файлов

```
src/
├── services/
│   └── sheetsService.ts          ← Загрузка данных из Google Sheets
│       ├── fetchProductsFromSheets()    - Fetch + parse CSV
│       ├── parseCSV()                   - Парсинг CSV
│       ├── fetchContactsFromSheets()    - Загрузка контактов
│       └── interfaces (SheetProduct, ContactInfo)
│
├── hooks/
│   ├── use-products.ts           ← React Query хук для товаров
│   │   ├── useProducts()               - Get all products
│   │   ├── useCategories()             - Get unique categories
│   │   └── useSizes()                  - Get all sizes
│   │
│   └── use-contacts.ts           ← React Query хук для контактов
│       └── useContacts()               - Get contacts
│
├── components/
│   ├── CatalogSection.tsx         ← Использует useProducts()
│   ├── NewArrivalsSection.tsx     ← Использует useProducts()
│   ├── ContactSection.tsx         ← Использует useContacts()
│   └── ... другие компоненты
│
└── data/
    └── products.ts               ← Fallback данные (если Google Sheets недоступна)

App.tsx                            ← Конфигурация React Query

netlify.toml                       ← Конфигурация Netlify (сборка, редиректы, headers)
.env.example                       ← Шаблон переменных окружения
.env.local                         ← Локальная переменная (не в GitHub)
```

## Типы данных

```typescript
// Данные из Google Sheets
interface SheetProduct {
  id: number;
  image: string;        // URL изображения
  name: string;
  price: number;
  oldPrice?: number;
  sizes: string;        // "S,M,L,XL" - перечисляются запятой
  category: string;
  isNew?: boolean;
}

// Данные приложения (внутренний формат)
interface Product {
  id: number;
  image: string;
  name: string;
  price: number;
  oldPrice?: number;
  sizes: Size[];        // ['S', 'M', 'L', 'XL'] - массив
  category: Category;
  isNew?: boolean;
}

// Контакты
interface ContactInfo {
  phone: string;
  email: string;
  telegram: string;
  instagram: string;
  address: string;
}
```

## React Query конфигурация

```typescript
// App.tsx
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,      // 5 минут
      gcTime: 1000 * 60 * 10,        // 10 минут
    },
  },
});

// staleTime: время, в течение которого кэш считается "свежим"
// После истечения стартует background refetch
// gcTime (ранее cacheTime): время, пока кэш хранится в памяти
```

## Парсинг CSV

```typescript
// Пример CSV из Google Sheets:
ID,Изображение,Название,Цена,Старая цена,Размеры,Категория,Новое?
1,https://img.jpg,"Пальто оверсайз",8990,12900,"S,M,L,XL",Пальто,Да

// Парсится в объект:
{
  id: 1,
  image: 'https://img.jpg',
  name: 'Пальто оверсайз',
  price: 8990,
  oldPrice: 12900,
  sizes: 'S,M,L,XL',  // строка
  category: 'Пальто',
  isNew: true,
}

// Затем конвертируется в Product:
{
  id: 1,
  image: 'https://img.jpg',
  name: 'Пальто оверсайз',
  price: 8990,
  oldPrice: 12900,
  sizes: ['S', 'M', 'L', 'XL'],  // массив
  category: 'Пальто' as Category,
  isNew: true,
}
```

## Переменные окружения

```env
# .env.local (для локальной разработки)
VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/{ID}/export?format=csv&gid=0

# Netlify Environment (для production)
VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/{ID}/export?format=csv&gid=0
# (Добавляется в Netlify → Site settings → Build & deploy → Environment)
```

## Обработка ошибок

```typescript
// Если Google Sheets недоступна:
async function useProducts() {
  try {
    const sheetData = await fetchProductsFromSheets();
    if (sheetData?.length > 0) {
      return sheetData;  // ✓ Успешно загружено
    }
  } catch (error) {
    console.error('Ошибка загрузки из Sheets:', error);
  }
  
  // Fallback к локальным данным
  return fallbackProducts;  // ← products.ts
}
```

## Сборка и развертывание

```
Git Push → GitHub
    ↓
GitHub Webhook → Netlify
    ↓
Netlify Build:
  1. Clone repository
  2. Install dependencies (bun install)
  3. Load environment variables
  4. Build: bun run build
  5. Output: dist/ folder
    ↓
Netlify Deploy:
  1. Upload dist/ to CDN
  2. Set up redirects (/pages/* → /index.html)
  3. Enable HTTPS/SSL
  4. Publish at https://your-site.netlify.app
    ↓
User accesses site → CDN serves static files → React hydrates → Data loads from Google Sheets
```

## Кэширование стратегия

```
Level 1: React Query (in-memory cache)
├─ staleTime: 5 min
├─ Automatic background refetch after 5 min
└─ Cleared when component unmounts (by default)

Level 2: Browser cache (HTTP caching)
├─ CSS/JS files: max-age=31536000 (1 year, immutable)
├─ HTML: max-age=0 (always revalidate)
└─ Configured in netlify.toml

Level 3: Netlify CDN cache
├─ Distributed globally
├─ Automatic purge on deploy
└─ Instant invalidation
```

## Performance

```
First Load:
├─ HTML: ~50KB (gzipped)
├─ CSS: ~100KB (gzipped)
├─ JS: ~200KB (gzipped)
├─ CSV от Google Sheets: ~10-100KB (зависит от размера)
└─ Total: ~350-450KB

Repeat Load (cached):
├─ All served from browser cache
├─ CSV loaded from React Query cache
└─ Time: <100ms

Network waterfall:
1. HTML (5ms)
2. CSS, JS (10ms)
3. CSV from Google Sheets (50-200ms)
4. Images (parallel, 100-500ms)
└─ Total: ~500-1000ms (отличный результат!)
```

## Масштабируемость

```
Товары в таблице:  Размер CSV:  Время парсинга:  Производительность:
100                ~5KB         <10ms             ✓ Excellent
500                ~25KB        ~20ms             ✓ Good
1000               ~50KB        ~50ms             ✓ Good
5000               ~250KB       ~100ms            ✓ Acceptable
10000              ~500KB       ~200ms            ⚠ Slow start
50000+             >2MB         >500ms            ✗ Needs optimization

Рекомендуемое количество товаров: <2000 для идеальной производительности
```

## Альтернативные подходы

### 1. Google Sheets API (более сложно)
✓ Лучший контроль
✓ Меньше данных передается
✗ Требует OAuth
✗ API квоты
✗ Сложнее настроить

### 2. Файлы в Git (для статического контента)
✓ Просто
✓ Быстро
✗ Требует пересборку для каждого изменения
✗ Не подходит для часто меняющихся данных

### 3. Полноценная БД (Firebase, Supabase)
✓ Профессионально
✓ Больше возможностей
✗ Платно
✗ Сложнее

## Логирование и отладка

```typescript
// sheetsService.ts имеет логирование
console.error('Ошибка загрузки данных из Google Sheets:', error);

// Браузерная консоль (F12):
// - Network tab: посмотрите запрос к Google Sheets
// - Console tab: ошибки и логи
// - React DevTools: посмотрите состояние хуков

// Netlify Logs:
// Netlify → Deploys → Deploy log (для build ошибок)
```

---

**Это полностью функциональная и масштабируемая архитектура для вашего проекта!** ✨
